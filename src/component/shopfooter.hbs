<hr class="mx-5">
<footer class="m-3">

    <div class="row">
        {{#each footer.[0]}}
        <div class="col-3">
            <div class="nav">
                <div class="nav-item">
                    <a href="{{link}}" class="btn btn-link nav-link">{{text}}</a>
                </div>
            </div>
        </div>
        {{/each}}

        {{#each footer.[1]}}
        <div class="col-3">
            <div class="nav">
                <div class="nav-item">
                    <a href="{{link}}" class="btn btn-link nav-link">{{text}}</a>
                </div>
            </div>
        </div>
        {{/each}}

        {{#each footer.[2]}}
        <div class="col-3">
            <div class="nav">
                <div class="nav-item">
                    <a href="{{link}}" class="btn btn-link nav-link">{{text}}</a>
                </div>
            </div>
        </div>
        {{/each}}

        {{#each footer.[3]}}
        <div class="col-3">
            <div class="nav">
                <div class="nav-item">
                    <a href="{{link}}" class="btn btn-link nav-link disabled">{{text}}</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</footer>